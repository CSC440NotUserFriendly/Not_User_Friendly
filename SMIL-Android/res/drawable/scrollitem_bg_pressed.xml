<!-- wondering about using RoundRectShape instead of Shape, may try it to see how it looks
- - http://developer.android.com/reference/android/graphics/drawable/shapes/RoundRectShape.html
- - but the corner tag inside the shape tag should do what I'm looking for -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:startColor="@color/scrollitem_pressed_bg_start"
        android:endColor="@color/scrollitem_pressed_bg_end"
        android:angle="-90"/>
    <corners android:radius="@dimen/scrollitem_radius" />
    <stroke android:width="1dp" android:color="@color/scrollitem_stroke" />
</shape>